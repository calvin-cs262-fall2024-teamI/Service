// TODO: add custom error types, and handle them in the error handler middleware.
// TODO: replace code like res.status(401).json(ApiResponse.error("Invalid credentials")); with throw new Error("Invalid credentials")
